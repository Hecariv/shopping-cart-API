import React from "react"


const Header = () => (
    <nav className="navbar navbar-dark bg-primary">
        <a className="navbar-brandn title" href="#">Shopping Cart</a>
    </nav>
)

export default Header;
